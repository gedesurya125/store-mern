import {makeStyles} from '@material-ui/core/styles';

export const useStyles = makeStyles({
	root: {
		background: 'red',
		zIndex: '-1'
	},
	stockContainer: {
		background: 'lightBlue',
	},

	paper: {
		padding: '1em',
	}


})	

